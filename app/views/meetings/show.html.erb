<div class="max-w-4xl mx-auto">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold text-gray-900">会議詳細</h1>
    <div class="space-x-2">
      <%= link_to "一覧に戻る", meetings_path, class: "text-gray-600 hover:text-gray-900" %>
      <%= button_to "削除", meeting_path(@meeting), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "text-red-600 hover:text-red-800" %>
    </div>
  </div>

  <div class="bg-white rounded-lg shadow p-6 mb-6">
    <h2 class="text-lg font-semibold text-gray-800 mb-4">会議情報</h2>
    <div class="grid grid-cols-2 gap-4">
      <div>
        <dt class="text-sm font-medium text-gray-500">評価者</dt>
        <dd class="mt-1 text-gray-900"><%= @meeting.evaluator.display_name %></dd>
      </div>
      <div>
        <dt class="text-sm font-medium text-gray-500">被評価者</dt>
        <dd class="mt-1 text-gray-900"><%= @meeting.evaluatee.display_name %></dd>
      </div>
      <div>
        <dt class="text-sm font-medium text-gray-500">ファイル名</dt>
        <dd class="mt-1 text-gray-900"><%= @meeting.original_filename %></dd>
      </div>
      <div>
        <dt class="text-sm font-medium text-gray-500">作成日時</dt>
        <dd class="mt-1 text-gray-900"><%= @meeting.created_at.strftime("%Y/%m/%d %H:%M") %></dd>
      </div>
    </div>
  </div>

  <% if @meeting.analysis_result.present? %>
    <div class="bg-white rounded-lg shadow p-6 mb-6">
      <h2 class="text-lg font-semibold text-gray-800 mb-4">AI解析結果</h2>

      <div class="grid grid-cols-2 gap-6">
        <div>
          <div class="mb-6">
            <h3 class="text-sm font-medium text-blue-600 mb-2">会議のサマリー</h3>
            <p class="text-gray-900 font-medium"><%= @meeting.analysis_result.summary %></p>
          </div>

          <div>
            <h3 class="text-sm font-medium text-blue-600 mb-2">次のステップ (育成・成長支援)</h3>
            <ul class="list-disc list-inside text-gray-700 space-y-1">
              <% @meeting.analysis_result.next_steps.each do |step| %>
                <li><%= step %></li>
              <% end %>
            </ul>
          </div>
        </div>

        <div>
          <h3 class="text-sm font-medium text-blue-600 mb-2">評価タグと5段階スコア</h3>
          <div class="space-y-3">
            <% @meeting.analysis_result.evaluation_tags.each do |tag| %>
              <% score = @meeting.analysis_result.score_by_tag[tag] || "N/A" %>
              <div class="flex justify-between items-center p-3 bg-gray-50 rounded">
                <span class="font-medium text-gray-700"><%= tag %></span>
                <span class="text-2xl font-bold text-blue-600"><%= score %> / 5</span>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <% if @meeting.transcription.present? %>
      <div class="bg-white rounded-lg shadow p-6">
        <h2 class="text-lg font-semibold text-gray-800 mb-4">文字起こし</h2>
        <div class="bg-gray-50 p-4 rounded whitespace-pre-wrap text-sm text-gray-700">
          <%= @meeting.transcription %>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6 text-center">
      <p class="text-yellow-700">
        AI解析を実行中です。しばらくお待ちください...
      </p>
      <p class="text-sm text-yellow-600 mt-2">
        ページを更新すると結果が表示されます
      </p>
    </div>
  <% end %>
</div>
